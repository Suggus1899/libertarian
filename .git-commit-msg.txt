feat: automatic translation with LibreTranslate

- Add lib/translate.ts: LibreTranslate integration with batch HTML
  translation (preserves HTML structure by translating text nodes only)
- Add translateArticle server action: translates an article to the
  other locale and saves/updates it in the DB
- Add TranslateButton component in admin dashboard (desktop table + mobile)
- Auto-translate on publish: when creating/updating a published article,
  if no translation exists in the other locale, it auto-translates silently
- Configurable via LIBRETRANSLATE_URL and LIBRETRANSLATE_API_KEY env vars
  (defaults to public https://libretranslate.com)

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
